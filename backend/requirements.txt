fastapi>=0.115
uvicorn[standard]>=0.34
python-multipart>=0.0.20
pydantic>=2.10
pydantic-settings>=2.7
python-dotenv>=1.0

# Groq SDK (free LLM API, OpenAI-compatible)
groq>=1.4.0

# Vector store + local embeddings (bundled ONNX MiniLM model, no extra API key)
chromadb==0.5.23

# Pinned for onnxruntime DLL compatibility on Windows (chromadb's default
# resolution can pick onnxruntime 1.23.x / numpy 2.x, which fails to load)
onnxruntime==1.18.1
numpy==1.26.4

# Document loaders
pypdf==5.1.0
python-docx==1.1.2

# Chunking
langchain-text-splitters==0.3.5
